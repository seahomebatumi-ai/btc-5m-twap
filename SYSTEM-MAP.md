Слияние проверил: `main` = `4b3723a`, `5ed667a` внутри. Ветку `tz-06-pfair-calibration` на origin не удалили — карта уже написана как «веток нет», удали её вместе с загрузкой.

Карта — ревизия `2026-09-12-b`. Существенное: новый якорь `A6` на прайсер, теперь любое ТЗ, которое что-то оценивает, обязано назвать, **чем** оценивало; `pfair.py` заморожен по той же причине. Находка о переуверенности лежит в §4 именно как находка, а не как результат гейта — статистика постфактум, она ничего не закрывает. Дефект 12 закрыт, открыты 15 (мой слабый гейт), 16 (натяжение §3/§4 на двадцати пяти интервалах) и 17 (`V5_determinism` после смены схемы манифеста).

ТЗ-07 ничего не оценивает — оно меряет. Кривая дисперсии фида на пятнадцати лагах, поправка `g(h)` вмораживается в `pfair.py` литералами; пересчёт на лету запрещён прямо в тексте, иначе прайсер в принципе нельзя проверить вне выборки. Ниже `tau = 60` поправка ровно единица: горизонт там 2.5 с и 0.09 с, то есть ниже собственного такта фида, и мерить нечего.

Главное в этом ТЗ — **§7: гейт ТЗ-08 зафиксирован дословно уже сейчас**, до того как случились интервалы, которые он будет судить. Три числа: Brier ниже константы на каждом `tau`, не более двух корзин вне точной 99% области, и `|λ̂ − 1| ≤ 0.15`, где `λ̂` — та самая шкала, что сегодня равна 1.50. Порог 0.15 выведен из §1.4 КАНОНа, а не выбран. Плюс регрессия: `tz06-calibration.py` не трогается и обязан воспроизвести шесть Brier ТЗ-06 до шестого знака, иначе BLOCKED.

Тестовых интервалов после `1789166400` на момент прогона ТЗ-06 было около 160 из нужных 400 — оценка, поэтому V8 печатает точное число. ТЗ-08 выпущу примерно через сутки; котировки Tier C копятся параллельно и сессии не требуют.

Удалить ветку `tz-06-pfair-calibration`.

## Что сделать
1. Одной загрузкой: `SYSTEM-MAP.md` в корень и `TZ-07-variance-time.md` в `CryptoTZ/`
2. В Claude Code **на VPS** (не в облачной сессии) отправить `EXECUTE TZ-07` — модель **Opus**
3. Прислать `CryptoReports/TZ-07-variance-time-report.md`
4. Слить pull request после моего вердикта
